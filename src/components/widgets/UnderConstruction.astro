<section class="ai-hero" aria-labelledby="hero-title">
	<div class="ai-hero__frame">
		<div class="ai-hero__kicker" aria-label="Site status metadata">
			<span>STATUS: ONLINE</span>
			<span>MODE: REVISION</span>
			<span>FOCUS: WEB SYSTEMS / AI TOOLS / PRODUCT WORK</span>
		</div>

		<div class="ai-hero__grid">
			<div class="ai-hero__main">
				<div class="ai-hero__name-block">
					<p class="ai-hero__label">IDENTITY / PERSONAL SITE</p>
					<h1 id="hero-title">THOMAS VERDERESI</h1>
					<p class="ai-hero__subtitle">
						software engineer / web systems / technical product work / AI tools for humans
					</p>
				</div>

				<div class="ai-hero__statement">
					<p>
						I build web systems, refactor haunted codebases,<br />
						and turn complex product ideas into working software.
					</p>
				</div>

				<div class="ai-hero__surface">
					<p>
						This site is a working surface:<br />
						portfolio, notebook, lab, mess.
					</p>
				</div>
			</div>

			<aside class="ai-hero__meta" aria-label="Working surface metadata">
				<p class="ai-hero__label">SURFACE.JSON</p>
				<dl>
					<div>
						<dt>surface</dt>
						<dd>portfolio / notebook / lab / mess</dd>
					</div>
					<div>
						<dt>state</dt>
						<dd>unfinished on purpose</dd>
					</div>
					<div>
						<dt>method</dt>
						<dd>ship / revise / document</dd>
					</div>
				</dl>
				<p class="ai-hero__signal">AI TOOLS FOR HUMANS</p>
			</aside>
		</div>
	</div>
</section>

<style>
	.ai-hero {
		--background: #f7f3ea;
		--foreground: #111111;
		--accent: #ff4fd8;
		--border: #111111;
		--shadow-offset: 8px;

		overflow-x: clip;
		border-bottom: 4px solid var(--border);
		background:
			linear-gradient(rgba(17, 17, 17, 0.08) 1px, transparent 1px),
			linear-gradient(90deg, rgba(17, 17, 17, 0.08) 1px, transparent 1px),
			var(--background);
		background-size: 32px 32px;
		color: var(--foreground);
	}

	.ai-hero__frame {
		width: min(100%, 1180px);
		margin: 0 auto;
		padding: clamp(2rem, 7vw, 5.5rem) 1rem clamp(2.75rem, 7vw, 6rem);
	}

	.ai-hero__kicker,
	.ai-hero__main,
	.ai-hero__meta {
		border: 3px solid var(--border);
		background: var(--background);
		box-shadow: var(--shadow-offset) var(--shadow-offset) 0 var(--border);
	}

	.ai-hero__kicker {
		display: flex;
		flex-wrap: wrap;
		gap: 0;
		width: fit-content;
		max-width: calc(100% - var(--shadow-offset));
		margin: 0 0 1rem;
		font-size: clamp(0.72rem, 1.7vw, 0.88rem);
		font-weight: 950;
		line-height: 1;
		text-transform: uppercase;
	}

	.ai-hero__kicker span {
		border-right: 3px solid var(--border);
		padding: 0.75rem 0.85rem;
	}

	.ai-hero__kicker span:last-child {
		border-right: 0;
		background: var(--accent);
		color: var(--foreground);
	}

	.ai-hero__grid {
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		gap: 1rem;
		align-items: start;
	}

	.ai-hero__main {
		max-width: calc(100% - var(--shadow-offset));
	}

	.ai-hero__name-block,
	.ai-hero__statement,
	.ai-hero__surface {
		padding: clamp(1rem, 4vw, 2rem);
	}

	.ai-hero__name-block,
	.ai-hero__statement {
		border-bottom: 3px solid var(--border);
	}

	.ai-hero__label {
		margin: 0 0 0.75rem;
		font-size: 0.76rem;
		font-weight: 950;
		line-height: 1.1;
		text-transform: uppercase;
	}

	.ai-hero h1 {
		max-width: 10ch;
		margin: 0;
		font-size: clamp(3.25rem, 14vw, 9.5rem);
		font-weight: 950;
		line-height: 0.82;
		text-transform: uppercase;
		overflow-wrap: anywhere;
	}

	.ai-hero__subtitle {
		display: inline-block;
		margin: clamp(1rem, 3vw, 1.4rem) 0 0;
		border: 3px solid var(--border);
		background: var(--accent);
		padding: 0.55rem 0.7rem;
		font-size: clamp(0.86rem, 2.1vw, 1.08rem);
		font-weight: 950;
		line-height: 1.25;
		text-transform: lowercase;
		box-shadow: 5px 5px 0 var(--border);
	}

	.ai-hero__statement p,
	.ai-hero__surface p {
		max-width: 820px;
		margin: 0;
		font-size: clamp(1.45rem, 4.4vw, 3.1rem);
		font-weight: 900;
		line-height: 1.02;
	}

	.ai-hero__surface {
		background:
			linear-gradient(90deg, var(--accent) 0 0.6rem, transparent 0.6rem),
			var(--background);
	}

	.ai-hero__surface p {
		padding-left: 1rem;
		font-size: clamp(1.15rem, 3vw, 2rem);
		line-height: 1.08;
	}

	.ai-hero__meta {
		width: min(100%, 28rem);
		max-width: calc(100% - var(--shadow-offset));
		padding: 1rem;
		transform: translateY(0.75rem);
	}

	.ai-hero__meta dl {
		margin: 0;
	}

	.ai-hero__meta div {
		display: grid;
		grid-template-columns: 5.5rem minmax(0, 1fr);
		gap: 0.75rem;
		border-top: 2px solid var(--border);
		padding: 0.75rem 0;
	}

	.ai-hero__meta dt,
	.ai-hero__meta dd,
	.ai-hero__signal {
		margin: 0;
		font-size: 0.88rem;
		font-weight: 950;
		line-height: 1.2;
		text-transform: uppercase;
	}

	.ai-hero__meta dt {
		color: var(--accent);
	}

	.ai-hero__signal {
		display: inline-block;
		margin-top: 0.75rem;
		border: 3px solid var(--border);
		background: var(--foreground);
		color: var(--background);
		padding: 0.55rem 0.7rem;
	}

	:global(.dark) .ai-hero {
		background:
			linear-gradient(rgba(255, 79, 216, 0.18) 1px, transparent 1px),
			linear-gradient(90deg, rgba(255, 79, 216, 0.18) 1px, transparent 1px),
			#111111;
		color: #f7f3ea;
	}

	:global(.dark) .ai-hero__kicker,
	:global(.dark) .ai-hero__main,
	:global(.dark) .ai-hero__meta {
		border-color: #f7f3ea;
		background-color: #111111;
		box-shadow: var(--shadow-offset) var(--shadow-offset) 0 #ff4fd8;
	}

	:global(.dark) .ai-hero__kicker span,
	:global(.dark) .ai-hero__name-block,
	:global(.dark) .ai-hero__statement,
	:global(.dark) .ai-hero__meta div,
	:global(.dark) .ai-hero__subtitle,
	:global(.dark) .ai-hero__signal {
		border-color: #f7f3ea;
	}

	:global(.dark) .ai-hero__subtitle,
	:global(.dark) .ai-hero__kicker span:last-child {
		box-shadow: 5px 5px 0 #f7f3ea;
		color: #111111;
	}

	:global(.dark) .ai-hero__surface {
		background:
			linear-gradient(90deg, #ff4fd8 0 0.6rem, transparent 0.6rem),
			#111111;
	}

	@media (max-width: 46rem) {
		.ai-hero__kicker {
			width: calc(100% - var(--shadow-offset));
		}

		.ai-hero__kicker span {
			width: 100%;
			border-right: 0;
			border-bottom: 3px solid var(--border);
		}

		.ai-hero__kicker span:last-child {
			border-bottom: 0;
		}

		.ai-hero__meta div {
			grid-template-columns: 1fr;
			gap: 0.25rem;
		}
	}

	@media (min-width: 56rem) {
		.ai-hero__grid {
			grid-template-columns: minmax(0, 1fr) minmax(16rem, 24rem);
			gap: 1.5rem;
		}

		.ai-hero__meta {
			transform: translate(-0.35rem, 3.5rem);
		}
	}
</style>
