---
const { stack, maxYears } = Astro.props;
---

{
	stack.map(({ title, years }) => {
		const width = Math.round((years / maxYears) * 100);
		console.log(width);
		return (
			<div class="mt-2 flex w-[90%] flex-col justify-center items-center mx-auto relative">
				<h3 class="text-lg  mb-1 font-bold text-center">{title}</h3>
				<div class={`w-full h-7 rounded-full bg-gray-300`}>
					<div
						class={`h-7 rounded-full rounded-r text-right pr-5 font-bold bg-primary-500 dark:bg-secondary-500 flex items-center justify-end text-white text-xs `}
						style={{ width: `${width}%` }}
					>
						{years} years
					</div>
				</div>
			</div>
		);
	})
}
